export interface OptionsType {
  id: number;
  name: string;
  text: string;
  link: string;
  blank?: boolean;
}

export interface GameListType {
  id: number;
  name: string;
  src: string;
  link: string;
}
